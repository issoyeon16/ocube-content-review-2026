# OCUBE 홈페이지 AI 작업 규칙

이 문서는 저장소 전체에 적용되는 프로젝트별 작업 규칙이다. AI와 개발자는 작업을 시작하기 전에 이 문서와 `docs/ai-development-handoff-guidelines.md`를 끝까지 읽는다.

## 1. 문서 우선순위

1. 이 `AGENTS.md`
2. `docs/ai-development-handoff-guidelines.md`
3. 사용자가 현재 작업에 첨부한 기능 기획과 디자인 자료
4. 저장소의 페이지 데이터, 화면 구현과 배포 설정
5. 외부 참고 사이트와 일반적인 개발 관례

문서와 실제 코드가 다르면 현재 배포 상태와 작업 대상 파일을 확인한다. 충돌로 결과가 크게 달라질 때는 임의로 확정하지 말고 사용자에게 영향 범위를 알린다.

## 2. 프로젝트 개요

- 프로젝트명: OCUBE Corporate Website Renewal
- 목적: 오큐브의 사업 역량, 산업 AI 솔루션, 글로벌 파트너와 회사 정보를 외부 고객에게 설명하는 기업 홈페이지
- 주요 사용자: 제조·자동차·공공 분야 고객, 기술·사업 파트너, 도입 검토자, 채용 및 회사 정보 탐색자
- 현재 단계: 콘텐츠·UI 개선 및 공개 검토
- 지원 환경: 데스크톱 웹과 모바일 웹
- 기준 언어·지역: 한국어, Asia/Seoul
- 공개 검토 URL: `https://issoyeon16.github.io/ocube-content-review-2026/`

### 핵심 사용자 가치

방문자가 오큐브의 경험과 기술력, 제공 가능한 사업·솔루션, 도입 근거와 문의 경로를 짧은 시간 안에 이해할 수 있어야 한다.

### 현재 작업 방향

- 확정된 전체 사이트맵을 유지한다.
- Solution 항목 수는 7개로 유지하되 명칭과 표현은 별도 검토할 수 있다.
- 상세 페이지의 기본 정보 구조는 Hero, Overview, 주요 기능, 기술·운영, Reference 흐름을 따른다.
- 회사의 경험과 기술력을 검증 가능한 개괄식 문장으로 전달한다.
- 기존 공개 참고 사이트와 이 검토용 GitHub Pages 사이트를 혼동하지 않는다.

## 3. 기술 구성

- Next.js 16 App Router
- React 19
- TypeScript 5, `strict: true`
- CSS
- GSAP, Swiper
- 정적 내보내기: Next.js `output: "export"`
- 배포: GitHub Actions와 GitHub Pages
- 배포 기준 브랜치: `main`
- 배포 base path: `/ocube-content-review-2026`

명시적인 승인 없이 새 라이브러리를 설치하거나 상태 관리, 스타일, 라우팅, 빌드 및 배포 체계를 교체하지 않는다.

## 4. 저장소 구조와 책임

```text
/
├─ src/
│  ├─ app/                 Next.js 페이지와 라우트
│  ├─ components/          화면·업무·공통 UI 컴포넌트
│  ├─ config/              내비게이션 등 전역 설정
│  ├─ data/                화면 콘텐츠와 정적 데이터
│  ├─ hooks/               화면 동작과 모션 훅
│  ├─ styles/              Next.js 페이지 스타일
│  └─ types/               TypeScript 타입
├─ public/
│  ├─ *.html               현재 배포되는 레거시 상세 페이지
│  └─ assets/              레거시 페이지와 공통 정적 자산
├─ docs/                   AI 개발·인수인계 문서
├─ .github/workflows/      GitHub Pages 배포
├─ next.config.ts          정적 내보내기와 base path 설정
└─ package.json            실행·검사·빌드 명령
```

### 배포 원본 구분

이 저장소는 Next.js 페이지와 정적 HTML 페이지가 함께 배포되는 혼합 구조다.

| 공개 영역                              | 실제 수정 원본                                           |
| -------------------------------------- | -------------------------------------------------------- |
| 홈 `/`                                 | `src/app/page.tsx`와 `src/components/home/`, `src/data/` |
| 회사 소개 `/about/`                    | `src/app/about/`와 `src/components/about/`               |
| 글로벌 파트너 `/business/global-tech/` | `src/app/business/global-tech/`와 관련 컴포넌트·데이터   |
| Business 상세 `*.html`                 | `public/business-*.html`                                 |
| Solution 상세 `*.html`                 | `public/solution-*.html`                                 |
| 회사 연혁·레퍼런스·오시는 길 `*.html`  | 해당 `public/*.html`                                     |
| 레거시 상세 페이지 스타일·이미지       | `public/assets/`                                         |

루트의 `*.html`과 `assets/`에는 이전 단계의 중복 또는 참고 파일이 있다. Next.js 정적 빌드는 `public/`의 파일을 `out/`으로 복사하므로, 현재 `.html` 공개 페이지 수정은 `public/`을 원본으로 한다. 사용자가 동기화를 명시적으로 요청하지 않는 한 루트 중복 파일을 함께 수정하지 않는다.

`src/app/solutions/[slug]` 라우트도 존재하지만 현재 헤더의 Solution 상세 링크는 `public/solution-*.html`을 사용한다. 링크 체계를 변경하기 전에는 두 구현을 합치거나 한쪽을 삭제하지 않는다.

## 5. 작업 시작 전 확인

1. 현재 요청과 관련 문서를 끝까지 읽는다.
2. `git status -sb`로 사용자 변경 사항을 확인한다.
3. 대상 URL의 실제 원본이 `src/`인지 `public/`인지 확인한다.
4. 비슷한 컴포넌트, 콘텐츠 데이터와 스타일 패턴을 찾는다.
5. 구현 범위와 검증 방법을 정한다.
6. 새 기술, 외부 권한 또는 공개 배포가 필요한지 판단한다.

기존 구현으로 해결할 수 있으면 기존 패턴을 사용한다. 관련 없는 리팩터링, 대량 포맷과 파일 이동을 함께 수행하지 않는다.

## 6. 구현 원칙

### Next.js 영역

- 페이지는 라우트 입력을 해석하고 화면을 조합한다.
- 반복 콘텐츠는 가능한 한 `src/data/`에서 관리한다.
- 화면 표현, 모션, 콘텐츠 데이터와 타입의 책임을 구분한다.
- 기존 `@/*` 경로 별칭과 컴포넌트·스타일 구조를 유지한다.
- 클라이언트 컴포넌트는 브라우저 API나 상호작용이 필요할 때만 사용한다.

### 정적 HTML 영역

- 대상 페이지의 기존 섹션 구조와 공통 `public/assets` 스타일을 우선 재사용한다.
- 이미지, CSS, JavaScript는 `public/` 기준 상대 경로가 정적 export에서도 유효한지 확인한다.
- 화면 하나를 수정하기 위해 공통 CSS 전체를 불필요하게 재작성하지 않는다.
- 접근 가능한 제목 계층, 이미지 대체 텍스트와 링크 목적을 유지한다.
- 제공된 디자인이나 이미지가 있으면 임의로 현대화하거나 재해석하지 않는다.

### 퍼블리싱 단순성

- 요구되지 않은 탭, 모달, URL, 애니메이션과 기능을 추가하지 않는다.
- CSS와 의미 있는 HTML로 해결 가능한 표현을 JavaScript로 재구현하지 않는다.
- 재사용 가능성이 불명확한 요소를 성급하게 공통화하지 않는다.
- 퍼블리싱 단계에서 미확정 API, 인증과 운영 데이터를 추측해 만들지 않는다.

## 7. 명명과 콘텐츠 기준

### 브랜드·솔루션 표기

- 회사 한글명: `오큐브`
- 영문 브랜드: `OCUBE`
- 솔루션명: `Cubeon`, `QData`, `QFactory`, `QAgent`, `QDrive`, `EVCP`, `QVision`
- 메뉴의 영문 카테고리 표기는 기존 정보 구조와 디자인에서 사용하는 대소문자를 유지한다.

새 이름을 확정하거나 전체 사이트에 일괄 반영하기 전에는 현재 링크, 메타데이터, 본문과 이미지 안의 표기에 미치는 영향을 보고한다.

### 코드·파일

- React 컴포넌트와 타입: PascalCase
- 함수, 변수와 훅: camelCase
- 파일과 URL slug: 기존 kebab-case 규칙
- 정적 자산: 의미를 알 수 있는 영문 kebab-case
- 사용자에게 보이는 문구는 한국어를 기본으로 하며 같은 개념에 같은 용어를 사용한다.

## 8. 정적 자산

- Next.js와 레거시 HTML에서 공개할 자산은 `public/` 아래에 둔다.
- 원본 참고 파일을 저장소에 중복 복사하지 않는다.
- 이미지에 실제 너비·높이와 목적에 맞는 `alt`를 제공한다.
- 대용량 이미지·영상 추가 전 기존 형식과 용량을 확인한다.
- `out/`, `.next/`, 비교 스크린샷과 임시 파일은 커밋하지 않는다.

## 9. 실행과 검증

```text
의존성 설치: npm ci
개발 서버: npm run dev
정적 검사: npm run lint
타입 검사: npm run typecheck
포맷 검사: npm run format:check
production build: npm run build
정적 미리보기: 별도 스크립트 없음. `out/` 결과물 또는 GitHub Pages에서 확인
```

현재 단위 테스트와 E2E 테스트 스크립트는 없다. 존재하지 않는 검증을 통과했다고 보고하지 않는다.

변경 범위에 맞춰 다음을 확인한다.

- `npm run lint`
- `npm run typecheck`
- `npm run build`
- 정적 HTML과 자산이 `out/`에 생성되는지 확인
- 대상 공개 URL과 내부 링크
- 데스크톱과 모바일 레이아웃
- 이미지 로딩, 대체 텍스트와 가로 넘침

## 10. 배포

`.github/workflows/deploy.yml`은 `main` push 시 `npm ci`와 `npm run build`를 실행하고 `out/`을 GitHub Pages에 배포한다.

- 커밋, push와 공개 배포는 사용자가 요청했을 때만 수행한다.
- 기존 참고 사이트나 다른 저장소에 배포하지 않는다.
- 배포 후 GitHub Actions 성공 여부와 실제 공개 페이지를 확인한다.
- 배포 URL을 변경하거나 별도 호스팅을 만들기 전에 사용자 승인을 받는다.

## 11. 보안과 변경 안전

- 토큰, API 키, 비밀번호와 실제 개인정보를 저장소에 넣지 않는다.
- 사용자 입력이나 외부 데이터를 신뢰하지 않는다.
- 관련 없는 사용자 변경을 수정·삭제·되돌리지 않는다.
- `git reset --hard`, 강제 push와 광범위 삭제를 사용하지 않는다.
- 외부 공개 문구는 확인 가능한 회사 자료를 근거로 작성하고, 수치·인증·고객명을 추측하지 않는다.

## 12. 완료 보고와 인수인계

작업 완료 시 다음 내용을 필요한 범위에서 정리한다.

```md
## 구현 결과

- 사용자가 확인하거나 사용할 수 있게 된 내용

## 주요 변경 파일

- 파일 경로: 역할

## 설계 판단

- 재사용한 기존 구조와 선택 이유

## 검증 결과

- 실행한 명령과 결과
- 확인한 URL과 화면 크기

## 남은 작업

- 미확정 콘텐츠, 디자인 또는 정책

## 프론트엔드 인수인계

- 다음 수정 시 확인할 원본 파일과 의존 관계
```

검증하지 못한 항목이 있으면 구현 완료와 검증 완료를 구분한다.

## 13. 현재 상태

- 전체 사이트맵은 확정 상태다.
- Business는 AX, Embedded, SI와 Global Tech 영역으로 구성된다.
- Solution은 7개로 고정돼 있으며 명칭은 재검토 가능하다.
- 홈, 글로벌 파트너, 회사 소개는 Next.js 구현이 중심이다.
- Business와 Solution 상세 일부는 `public/*.html` 정적 페이지가 현재 공개 원본이다.
- 공개 검토 사이트는 GitHub Pages에서 운영한다.
- 콘텐츠·명명·화면 자료를 단계적으로 보강하는 중이다.
